[Service]
# Forwarded host/scheme headers are trusted only because Caddy is the sole
# network-facing HTTP server after this override is installed.
ExecStart=
ExecStart=/opt/print-queue-manager/venv/bin/gunicorn --bind 127.0.0.1:5000 --workers 2 --timeout 120 app:app
